{
  "name": "pediatric-case-summary-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker for AI API proxy",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.2.2",
    "wrangler": "^3.19.0"
  }
}
